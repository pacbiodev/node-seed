/// <reference path="../../typings/node/node.d.ts" />/// <reference path="../../typings/express/express.d.ts" />/// <reference path="../../typings/express-validator/express-validator.d.ts" />/// <reference path="../../typings/winston/winston.d.ts" />declare var config;// Include these filesrequire('../includes/object');require('../includes/string.js');import Express = require('express');import ExpressValidator = require('express-validator');import Logger = require('winston');import User = require('../controllers/user');var router = Express.Router();router.get('/logout',          (req:ExpressValidator.Request,           res:Express.Response,           next:Function) => {            req.user = null;            res.status(200)               .json({ token: null, user: null });          });router.post('/login/with/:username/and/:password',            (req:ExpressValidator.Request,             res:Express.Response,             next:Function) => {              req.checkParams('username', 'username is required').notEmpty();              req.checkParams('password', 'password is required').notEmpty();              var errors = req.validationErrors(false);              if ((errors!=null) &&                  (errors.length>0)) {                res.status(400)                   .json(errors);              } else {                User.Controller                    .login(req.params.username,                           req.params.password,                           (result) => {                             res.status(result.status)                                .json(result.content);                           });              }            });module.exports = router;